import React from 'react';
import { Text } from 'react-native';

export const Copy = () => {
  return (
    <Text
      style={{
        color: 'white',
        fontSize: 28,
        marginBottom: 20,
      }}
    >
      Edit src/App.tsx and save to reload.
    </Text>
  );
};
